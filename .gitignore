__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/
*.db
*.db-journal
.coverage
htmlcov/

# The SQLite DB and the full raw sweep CSV are regenerated by `fraud-detector
# run` in seconds; the committed evidence a reader sees without re-running
# anything is reports/cost_vs_threshold.png, reports/sensitivity.png, and
# reports/sensitivity.csv (small, human-readable).
reports/fraud.db
reports/threshold_sweep.csv
